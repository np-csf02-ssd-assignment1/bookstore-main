@page 
@model WebFrontend.Pages.Carts.EditModel

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Edit</h1>

<h4>Cart</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Cart.CartId" />
            <input type="hidden" asp-for="Cart.UserId" />
            <input type="hidden" asp-for="Cart.DateCreated" />
            <input type="hidden" asp-for="Cart.ProductId" />
            <div class="form-group">
                <label asp-for="Cart.Quantity" class="control-label"></label>
                <input asp-for="Cart.Quantity" class="form-control" />
                <span asp-validation-for="Cart.Quantity" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to Cart</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
